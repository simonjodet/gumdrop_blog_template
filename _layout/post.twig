{% extends "default.twig" %}

{% block content %}
    <style type="text/css">
        div.below_fold {
            display: block;
        }
    </style>
    <div class="post_container">
        <div class="page-header post-title">
            <h3>
                {{ page.title }}
                <small>Published on {{ page.date|date("Y-m-d") }}.</small>
            </h3>
        </div>
        <div style="margin-top:5px;">
            {{ content }}
        </div>
    </div>
    <!--PLACE DISQUS CODE HERE-->
{% endblock %}
